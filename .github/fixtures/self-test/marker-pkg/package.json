{
  "name": "self-test-marker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "postinstall": "node -e \"require('fs').writeFileSync(process.env.MARKER_FILE || '/tmp/self-test-marker', 'ran')\""
  }
}
