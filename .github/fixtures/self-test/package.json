{
  "name": "base-ci-self-test-fixture",
  "version": "0.0.0",
  "private": true,
  "description": "Fixture exercised by .github/workflows/self-test.yml. self-test-marker writes a sentinel file from its postinstall script, so we can assert --ignore-scripts truly skipped it.",
  "dependencies": {
    "self-test-marker": "file:./marker-pkg"
  },
  "trustedDependencies": ["self-test-marker"]
}
